/*import './Item.css'
import { Link } from 'react-router-dom'

const Item = ({id, name, img, price}) => {
    
    return (
        <article className="CardItem">
            <header classeName="Header">
                <h2 classeName="ItemHeader">
                    {name}
                </h2>
                </header>
                <picture>
                    <img src={img} alt={name} className="ItemImg"/>
                    </picture>
                    <section>
                        <p classe name="Info">
                            Precio: ${price}
                        </p>
                    </section>
                    <footer classeName='ItemFooter'>
                        <link to='/detail'>Ver detalle</link>
                    </footer>
        </article>
    )
}

export default Item  */